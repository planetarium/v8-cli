'use strict';

// AUTO-GENERATED by scripts/codegen.js — do not edit manually

const { get, post, put } = require('../api');
const { getBaseUrl } = require('../config');
const { table, ok, err, requireToken } = require('../output');

function registerMissions(program) {
  const cmd = program.command('missions').description('missions management');

  // POST /v1/admin/missions/calculate-ranks
  // Manually trigger creator mission rank calculation
  cmd
    .command('calculate-ranks')
    .description('Manually trigger creator mission rank calculation')
    .action(async function () {
      const config = this.parent.parent._v8config;
      requireToken(config);
      const base = getBaseUrl(config);
      const res = await post(base, `/v1/admin/missions/calculate-ranks`, config.token, {});
      if (res.status !== 200 && res.status !== 201) return err(`${res.status}: ${res.data.message || 'failed'}`);
      if (this.parent.parent.opts().json) return console.log(JSON.stringify(res.data));
      ok(JSON.stringify(res.data, null, 2));
    });

}

module.exports = { registerMissions };
