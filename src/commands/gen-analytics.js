'use strict';

// AUTO-GENERATED by scripts/codegen.js — do not edit manually

const { get, post, put } = require('../api');
const { getBaseUrl } = require('../config');
const { table, ok, err, requireToken } = require('../output');

function registerAnalytics(program) {
  const cmd = program.command('analytics').description('analytics management');

  // POST /v1/admin/analytics/update-trending-scores
  // Manually trigger trending scores update calculation
  cmd
    .command('update-trending-scores')
    .description('Manually trigger trending scores update calculation')
    .action(async function () {
      const config = this.parent.parent._v8config;
      requireToken(config);
      const base = getBaseUrl(config);
      const res = await post(base, `/v1/admin/analytics/update-trending-scores`, config.token, {});
      if (res.status !== 200 && res.status !== 201) return err(`${res.status}: ${res.data.message || 'failed'}`);
      if (this.parent.parent.opts().json) return console.log(JSON.stringify(res.data));
      ok(JSON.stringify(res.data, null, 2));
    });

  // POST /v1/admin/analytics/calculate-quality-scores
  // Manually trigger quality scores calculation (Q, Q_final, Rising Score, Hot New Score)
  cmd
    .command('calculate-quality-scores')
    .description('Manually trigger quality scores calculation (Q, Q_final, Rising Score, Hot New S')
    .action(async function () {
      const config = this.parent.parent._v8config;
      requireToken(config);
      const base = getBaseUrl(config);
      const res = await post(base, `/v1/admin/analytics/calculate-quality-scores`, config.token, {});
      if (res.status !== 200 && res.status !== 201) return err(`${res.status}: ${res.data.message || 'failed'}`);
      if (this.parent.parent.opts().json) return console.log(JSON.stringify(res.data));
      ok(JSON.stringify(res.data, null, 2));
    });

}

module.exports = { registerAnalytics };
